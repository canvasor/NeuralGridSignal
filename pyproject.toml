[project]
name = "neural-grid-signal"
version = "0.1.0"
description = "OKX futures grid signal assistant for nofx importable strategy JSON."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
